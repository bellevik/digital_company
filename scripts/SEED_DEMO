#!/usr/bin/env bash

set -euo pipefail

source "$(cd "$(dirname "$0")" && pwd)/_common.sh"

echo "Seeding demo data..."
curl --fail --silent --show-error \
  -X POST \
  "$(api_url localhost)/api/v1/operations/seed-demo"
echo

